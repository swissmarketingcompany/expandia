{
  "name": "expandia-web",
  "version": "1.0.0",
  "description": "Expandia AI Solutions Website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:css": "tailwindcss -i ./src/css/input.css -o ./dist/css/output.css",
    "build:js": "mkdir -p dist/js && cp src/js/*.js dist/js/",
    "build:pages": "node build-pages.js",
    "build": "npm run build:css && npm run build:js && npm run build:pages",
    "heroku-postbuild": "npm run build",
    "test": "echo 'No tests specified'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.13",
    "buffsend-mcp": "^1.0.1",
    "daisyui": "^4.12.10",
    "express": "^4.18.2",
    "tailwindcss": "^3.4.0"
  },
  "keywords": [
    "ai",
    "solutions",
    "business",
    "expandia",
    "heroku"
  ],
  "author": "Expandia",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/expandia.git"
  }
}
